version: "3.7"

services:
  mysql:
    image: mysql
    container_name: mysql
#    networks:
#      - backend
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "MissingCalls!1"
    volumes:
      - "./DB:/var/lib/mysql"

  rabbit:
    image: rabbitmq:3-management
    container_name: queue
#    networks:
#      - backend
    environment:
      RABBITMQ_ERLANG_COOKIE: "SWQOKODSQALRPCLNMEQG"
      RABBITMQ_DEFAULT_USER: "rabbitmq"
      RABBITMQ_DEFAULT_PASS: "rabbitmq"
      RABBITMQ_DEFAULT_VHOST: "/"
    ports:
      - "15672:15672"
      - "5672:5672"
    volumes:
      - "./conf/rabbit/enabled_plugins:/etc/rabbitmq/enabled_plugins"

#  api:
#    image: goapi
#    container_name: goapi
#    networks:
#      - backend
#      - frontend
#    ports:
#      - "8086:8086"
#    volumes:
#      - "./conf/gofsync:/conf"
#    depends_on:
#      - mysql
#      - rabbit
#    command: ["/go/bin/gofsync", "-hosts", "conf/hosts", "-conf", "config", "-server"]

#networks:
#  frontend:
#  backend:

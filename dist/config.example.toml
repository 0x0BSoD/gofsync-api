[HOSTS]
file = "./hosts"

[API]
# this creds be update after user login
username = "user"
password = "pass"
get_per_page = 100

[RT]
pro = "rt.ringcentral.com"
stage = "rt.stage.ringcentral.com"

[DB]
db_provider = "mysql"
db_user = "root"
db_password = "root123"
db_schema = "goFsync"

[WEB]
port = 8080
jwt_secret = "SDKF@09jd2j)9jf@sharkJR0!sdascsd"

# Values => DISCARD | STDOUT | STDERR | path to file
[LOGGING]
err_log = "STDERR"
acc_log = "STDOUT"
trace_log = "DISCARD"

[LDAP]
bin_user = "user"
bin_pass = "pass"
ldap_server = "server"
ldap_server_port = 389
base_dn = "ou=RingCentral Admins,dc=ringcentral,dc=com"
match_string = "(&(objectClass=organizationalPerson)(uid=%s))"

